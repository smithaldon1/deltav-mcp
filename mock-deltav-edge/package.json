{
  "name": "mock-deltav-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "dev": "npm --prefix ./ui run build && tsx watch src/server.ts",
    "build": "npm --prefix ./ui run build && tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
